$("#<%=vote_comment.id%>").text("<%= "#{vote_comment.sum_votes} (#{vote_comment.sum_down_votes})" %>");
<% if (vote_comment.abusive && !post_owner?) %>
  $("#<%=vote_comment.id%>").closest("tr").hide();
<% end %>